@page
@model ExcelRead.Pages.ToDos.EditModel
@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ToDo.Id" />
            <div class="form-group">
                <label asp-for="ToDo.Title" class="control-label"></label>
                <input asp-for="ToDo.Title" class="form-control" />
                <span asp-validation-for="ToDo.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ToDo.IsCompleted" class="control-label"></label>
                <input asp-for="ToDo.IsCompleted" class="form-control" />
                <span asp-validation-for="ToDo.IsCompleted" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ToDo.UserId" class="control-label"></label>
                <input asp-for="ToDo.UserId" class="form-control" />
                <span asp-validation-for="ToDo.UserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

